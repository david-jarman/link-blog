<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="author" content="David Jarman" />
    <base href="/" />
    <link rel="alternate" href="/atom/all" type="application/atom+xml" title="David Jarman's Blog" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["LinkBlog.Web.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="favicon.ico" />
    <script>
        // Inline script to set theme + style before page renders (prevents FOUC)
        (function() {
            const theme = localStorage.getItem('theme-preference') ||
                (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
            const style = localStorage.getItem('style-preference') || 'modern';
            document.documentElement.setAttribute('data-theme', theme);
            document.documentElement.setAttribute('data-style', style);
        })();
    </script>
    <HeadOutlet />
</head>

<body>
    <Routes  />
    @* Disable enhanced navigation, which causes issues when writing javascript to react to page load events *@
    <script src="_framework/blazor.web.js" autostart="false"></script>
    <script>
        Blazor.start({
            ssr: { disableDomPreservation: true }
        });
    </script>
    <script src="@Assets["theme.js"]"></script>
</body>

</html>
